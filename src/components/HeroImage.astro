---
const { src, alt = "", height = 240 } = Astro.props;
---

<section style="margin-bottom: 1.5rem;">
  <img
    src={src}
    alt={alt}
    style={`
      width: 100%;
      height: ${height}px;
      object-fit: cover;
      border-radius: 8px;
    `}
  />
</section>
